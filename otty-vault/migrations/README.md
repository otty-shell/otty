# `otty-vault` migration assets

This directory is reserved for versioned SQL migration assets that will back
the encrypted vault schema in the next implementation tasks.

Foundation task 01 only fixes the public contract, schema versioning boundary,
and migration runner integration point.
