[package]
name = "otty"
version = "0.1.0"
edition = { workspace = true }
rust-version = { workspace = true }
license = "Apache-2.0"

[dependencies]
iced = { version = "0.13", features = ["canvas", "lazy", "advanced", "svg"] }
otty-ui-term = { path = "../otty-ui/terminal", version = "0.1.0" }
